{
  "name": "datasist-frontend-static",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "cf:dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name datasist-frontend --branch main --commit-dirty=true",
    "cf:deploy": "wrangler pages deploy . --project-name datasist-frontend --branch main --commit-dirty=true"
  },
  "devDependencies": {
    "wrangler": "^4.80.0"
  }
}
